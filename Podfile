# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Train Tracked' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Train Tracked

	pod 'Firebase/Core'

  target 'Train TrackedTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Train TrackedUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
